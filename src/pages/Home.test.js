import { render, screen } from "@testing-library/react";
import Home from "./Home";

describe("Home Component", () => {
  test("all launches data is there or not", () => {
    // Arrange
    render(<Home />);

    // Act
    // Do nothing

    // Assert
    // const launchsElement = screen.
    // expect(launchsElement)
  });
});
